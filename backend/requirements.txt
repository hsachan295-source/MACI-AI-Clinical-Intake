# ---------------------------------------------------------------------------
# MACI backend - RUNTIME dependencies (used by Vercel / production).
# Keep this lean: no test runners, no compiled servers, no unused libs.
# Dev/local extras (uvicorn, pytest, ...) live in requirements-dev.txt.
# ---------------------------------------------------------------------------

fastapi==0.115.6
python-multipart==0.0.20
pydantic>=2.11.7,<3.0.0
pydantic-settings>=2.7.0,<3.0.0

# HTTP client for Groq / OCR.Space (both called server-side only)
httpx>=0.27,<0.29

# .env support for local dev (unused on Vercel; tiny, pure-python)
python-dotenv==1.0.1

# --- Optional integrations -------------------------------------------------
# Imported lazily and only when their keys are set. If an import or a network
# call fails, the app degrades to an in-memory / offline fallback and keeps
# serving - it never crashes at import or startup.
supabase==2.31.0
pinecone==5.4.2
